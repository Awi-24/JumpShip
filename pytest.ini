[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --no-header
markers =
    llm_integration: marks tests that require a live Ollama instance (deselect with -m "not llm_integration")
